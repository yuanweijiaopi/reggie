<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itheima.reggie.mapper.EmployeeMapper">

    <insert id="register">
        INSERT INTO reggie.employee
            (username, password,
             create_time,
             update_time,
             create_user,
             update_user
            )
        VALUES(
                  #{username},
                  #{password},
                  #{createTime},
                  #{updateTime},
                  #{createUser},
                  #{updateUser}
              )
    </insert>
</mapper>